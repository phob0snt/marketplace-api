# 🛒 Marketplace API

Marketplace API - это полноценная backend-система, построенная на микросервисной архитектуре. Проект включает аутентификацию пользователей, управление каталогом товаров, корзину покупок и систему заказов.

### Ключевые особенности

✨ **Автоматическая инициализация** - администратор и 15 товаров создаются при первом запуске  
🔐 **JWT аутентификация** - безопасная система авторизации с refresh токенами  
📦 **Микросервисная архитектура** - независимые сервисы для auth и orders  
🚀 **Готов к использованию** - Docker Compose для запуска одной командой  
📚 **Swagger UI** - интерактивная документация API  
🔄 **Redis кеширование** - быстрая работа с сессиями  

## 🔧 Конфигурация

### Переменные окружения

Переименуйте `.env.development` файл в `.env` и, при необходимости, измените его:

```env
DATABASE_URL=postgresql://user:password@host:5432/marketplace_auth
REDIS_URL=redis://redis:6379/0
JWT_SECRET_KEY=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
ADMIN_LOGIN=admin
ADMIN_PASSWORD=admin123

DATABASE_URL=postgresql://user:password@host:5432/marketplace_orders
```

## 🚀 Быстрый старт

### Предварительные требования

- Docker `20.10+`
- Docker Compose `2.0+`

### Запуск проекта

```bash
# 1. Клонировать репозиторий
git clone https://github.com/phob0snt/marketplace-api.git
cd marketplace-api

# 2. Запустить все сервисы
docker-compose up -d

# Готово! Сервисы автоматически:
# ✓ Применят миграции БД
# ✓ Создадут администратора (admin/admin123)
# ✓ Загрузят 15 товаров
```

### Линт кода (ruff)

Установите ruff и запустите проверки:
```bash
pip install ruff

cd /путь/к/проекту/marketplace-api

ruff check .
ruff format --check .
```

## 🛠 Технологический стек

### Backend
- **FastAPI** `0.104+` - Современный асинхронный веб-фреймворк
- **SQLAlchemy** `2.0+` - ORM для работы с базой данных
- **Alembic** - Миграции схемы базы данных
- **Pydantic** `2.0+` - Валидация данных и сериализация

### Безопасность
- **JWT (PyJWT)** - JSON Web Tokens для аутентификации
- **Passlib + Bcrypt** - Хеширование паролей
- **Python-JOSE** - Обработка JWT токенов

### База данных
- **PostgreSQL** `14+` - Реляционная СУБД
- **Redis** `7+` - In-memory хранилище для сессий и кеша

### DevOps
- **Docker + Docker Compose** - Контейнеризация и оркестрация
- **Uvicorn** - ASGI сервер для FastAPI



### Проверка работы

```bash
# Проверить статус контейнеров
docker-compose ps

# Посмотреть логи
docker-compose logs -f
```

**Swagger UI доступен по адресам:**
- Auth Service: http://localhost:8081/docs
- Orders Service: http://localhost:8082/docs

## 📦 Автоматически созданные моковые данные

### 👤 Администратор

| Поле | Значение |
|------|----------|
| Логин | `admin` |
| Пароль | `admin123` |
| Роль | `admin` |

### 👤 Пользователь

| Поле | Значение |
|------|----------|
| Логин | `test_user` |
| Пароль | `password123` |
| Роль | `user` |

Учетные данные можно изменить через переменные окружения:
```env
ADMIN_LOGIN=your_admin
ADMIN_PASSWORD=your_password
```

### 🛍 Товары (15 штук)

| ID | Название | Цена | Остаток |
|----|----------|------|---------|
| 1 | Laptop Dell XPS 15 | 150,000₽ | 15 шт |
| 2 | iPhone 15 Pro | 120,000₽ | 30 шт |
| 3 | Sony WH-1000XM5 | 35,000₽ | 50 шт |
| 4 | Mechanical Keyboard Keychron K8 | 8,000₽ | 100 шт |
| 5 | Logitech MX Master 3S | 9,500₽ | 75 шт |
| 6 | Samsung 27" 4K Monitor | 45,000₽ | 25 шт |
| 7 | Apple AirPods Pro 2 | 25,000₽ | 60 шт |
| 8 | USB-C Hub 7-in-1 | 3,500₽ | 200 шт |
| 9 | iPad Air 5th Gen | 75,000₽ | 20 шт |
| 10 | Samsung SSD 1TB | 12,000₽ | 80 шт |
| 11 | Webcam Logitech C920 | 7,500₽ | 45 шт |
| 12 | Gaming Chair | 25,000₽ | 15 шт |
| 13 | Microphone Blue Yeti | 15,000₽ | 35 шт |
| 14 | Desk Lamp LED | 4,500₽ | 120 шт |
| 15 | Nintendo Switch OLED | 35,000₽ | 40 шт |

## 🏗 Архитектура проекта

```
marketplace-api/
├── auth-service/              # Сервис аутентификации
│   ├── app/
│   │   ├── api/              # API endpoints
│   │   │   └── auth.py       # Регистрация, логин
│   │   ├── core/             # Ядро приложения
│   │   │   ├── config.py     # Настройки
│   │   │   ├── security.py   # JWT и хеширование
│   │   │   └── init_db.py    # Инициализация данных
│   │   ├── models/           # SQLAlchemy модели
│   │   │   └── auth.py       # Модель пользователя
│   │   ├── schemas/          # Pydantic схемы
│   │   ├── services/         # Бизнес-логика
│   │   │   ├── auth.py       # Аутентификация
│   │   │   └── redis_client.py # Redis операции
│   │   ├── repository/       # Работа с БД
│   │   └── db/               # Подключение к БД
│   ├── migration/            # Alembic миграции
│   └── Dockerfile
│
├── orders-service/           # Сервис заказов
│   ├── app/
│   │   ├── api/              # API endpoints
│   │   │   ├── product.py    # CRUD товаров
│   │   │   ├── cart.py       # Управление корзиной
│   │   │   ├── cart_item.py  # Элементы корзины
│   │   │   └── order.py      # Управление заказами
│   │   ├── core/             # Ядро приложения
│   │   │   ├── config.py     # Настройки
│   │   │   ├── auth.py       # Проверка JWT
│   │   │   └── init_db.py    # Загрузка товаров
│   │   ├── models/           # SQLAlchemy модели
│   │   │   ├── product.py    # Модель товара
│   │   │   ├── cart.py       # Модель корзины
│   │   │   ├── cart_item.py  # Элемент корзины
│   │   │   ├── order.py      # Модель заказа
│   │   │   └── order_item.py # Элемент заказа
│   │   ├── schemas/          # Pydantic схемы
│   │   ├── services/         # Бизнес-логика
│   │   ├── repository/       # Работа с БД
│   │   └── db/               # Подключение к БД
│   ├── migration/            # Alembic миграции
│   └── Dockerfile
│
└── docker-compose.yml        # Оркестрация сервисов
```

## 🔌 API Endpoints

### Auth Service (`:8081`)

#### Публичные эндпоинты

```bash
POST /api/register          # Регистрация нового пользователя
POST /api/login             # Вход в систему
POST /api/refresh           # Обновление access токена
```

#### Защищенные эндпоинты

```bash
GET  /api/me                # Информация о текущем пользователе
POST /api/logout            # Выход (инвалидация токена)
```

### Orders Service (`:8082`)

#### Товары

```bash
GET    /products              # Список товаров (публичный)
GET    /products/{id}         # Получить товар (публичный)
POST   /products              # Создать товар (admin)
PATCH  /products/{id}         # Изменить количество (admin)
DELETE /products/{id}         # Удалить товар (admin)
```

#### Корзина

```bash
POST   /cart                  # Создать корзину (user)
GET    /cart                  # Получить корзину (user)
POST   /cart/add              # Добавить товар (user)
GET    /cart/items            # Список товаров в корзине (user)
GET    /cart/items/{id}       # Получить товар из корзины (user)
PATCH  /cart/items/{id}/change_quantity  # Изменить количество (user)
DELETE /cart/items/{id}       # Удалить товар (user)
```

#### Заказы

```bash
POST   /orders                # Создать заказ из корзины (user)
GET    /orders                # Список заказов пользователя (user)
GET    /orders/{id}           # Получить заказ (user)
POST   /orders/{id}/cancel    # Отменить заказ (user)
POST   /orders/{id}/complete  # Завершить заказ (admin)
```